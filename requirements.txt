torch==2.2.0
torchvision==0.17.0
torchaudio==2.2.0
--extra-index-url https://download.pytorch.org/whl/cu118
diffusers==0.32.2
transformers==4.48.0
numpy==1.26.4
accelerate==0.28.0
librosa==0.11.0
soundfile==0.12.1
opencv-python==4.9.0.80
einops==0.8.1
gradio==5.24.0
huggingface_hub==0.30.2
omegaconf==2.3.0
ffmpeg-python==0.2.0
imageio==2.31.1
gdown
requests
imageio[ffmpeg]

# GPEN-BFR Face Enhancement Dependencies (ONNX-based)
onnx>=1.17.0
onnxruntime-gpu>=1.22.0  # GPU acceleration for GPEN-BFR enhancement
scipy>=1.15.0
psutil>=7.0.0

# Fallback for systems without CUDA
# If onnxruntime-gpu fails, manually install: pip install onnxruntime>=1.22.0

# TalkNet Active Speaker Detection Dependencies (Hybrid Implementation)
# ============================================================================
# HYBRID APPROACH: TalkNet audio/visual processing + YOLOv8 face detection
# This avoids CMake/dlib dependency issues while maintaining ASD effectiveness
# ============================================================================

# Core TalkNet Dependencies (✅ Successfully installed)
pretrainedmodels>=0.7.4              # Pre-trained CNN architectures for TalkNet
resampy>=0.4.2                       # High-quality audio resampling for TalkNet
webrtcvad>=2.0.10                    # Google WebRTC Voice Activity Detection
scikit-learn>=1.1.0                  # ML utilities for feature processing
scikit-image>=0.19.0                 # Image processing for visual features

# Dependencies already satisfied by existing requirements:
# - torch/torchaudio (TalkNet neural networks)
# - numpy (numerical operations)
# - opencv-python (video processing)
# - librosa (audio feature extraction)
# - scipy (signal processing)
# - tqdm (progress tracking)

# DISABLED Dependencies (require CMake - not Windows-friendly):
# dlib>=19.24.0                      # Face detection (replaced by YOLOv8)
# face-alignment>=1.3.5              # Face landmarks (using YOLOv8 landmarks instead)

